/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HighlightCursorFollowerDirective } from './highlight-cursor-follower.directive';

describe('HighlightCursorFollowerDirective', () => {
  it('should create an instance', () => {
    let directive = new HighlightCursorFollowerDirective();
    expect(directive).toBeTruthy();
  });
});
