/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HighlightCursorDirective } from './highlight-cursor.directive';

describe('HighlightCursorDirective', () => {
  it('should create an instance', () => {
    let directive = new HighlightCursorDirective();
    expect(directive).toBeTruthy();
  });
});
